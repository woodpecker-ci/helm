apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "woodpecker.fullname" . }}-test-connection"
  labels:
    {{- include "woodpecker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "woodpecker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
